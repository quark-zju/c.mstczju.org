#include <stdio.h>
int main()
{
	int n,i,t;
	scanf("%d",&n);
	for (i=0;i<n;i++)
	{
		scanf("%d",&t);
		if (t>=1)
			printf("%d\n",t*(t+1)/2);
		else
			printf("%d\n",(t+1)*(1-t+1)/2);
	}
}

