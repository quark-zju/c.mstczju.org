#include <stdio.h>
#define M 190
#define L 1000

char s[M][L];
int f, r, n;

int main(int argc, char const* argv[])
{
	scanf("%d ", &n);
	while(gets(s[f])) if (s[f][1]) f =  (f+1) % M; else r = (r+1)%M;
	puts(s[(r + n - 3) % M]);
	
	return 0;
}
