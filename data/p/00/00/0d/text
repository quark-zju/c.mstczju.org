<p><b>提示</b></p>
<p style="color: red;">
在您桌面上试机说明中可以找到该题目的详细解答说明, 建议阅读说明后再完成该题, 以熟悉竞赛的评测系统.
</p>

<p><b>题目描述</b></p>
<p>
给定一个整数 <var>n</var>, 请输出 1 到 <var>n</var> 之间所有整数 (包括 1 和 <var>n</var>) 的和.
</p>

<p><b>输入格式</b></p>
<p>
输入的第一行是一个正整数 <var>T</var>, 表示有 <var>T</var> 组测试数据. 接下来一共有 <var>T</var> 行, 每行一个整数 <var>n</var>, 即你需要输出的和的区间的范围. 题目保证 <var>T</var> 不超过 100, 并且所输出的和不会超过 10000.
</p>

<p><b>输出格式</b></p>
<p>
输出一共有 <var>T</var> 行, 每行一个整数, 其中第 <var>i</var> 行表示第 <var>i</var> 组数据的求和结果.
</p>

<p><b>样例输入</b></p>
<pre>
3
5
10
20
</pre>

<p><b>样例输出</b></p>
<pre>
15
55
210
</pre>

<p><b>样例说明</b></p>
<p>
样例输入要求你的程序按顺序输出 3 行. 第一行为 1 到 5 之间所有整数的和, 即 15, 第二行为 1 到 10 之间所有整数的和, 为 55, 同理, 第三行的输出为 210.
</p>
<p>
请注意:
</p>
<ol>
    <li>请严格按照样例所示的格式输出. 任何多输出的内容, 包括任何多余字符, 空格等, 都会导致你的提交被判为错误.</li>
    <li>评测系统并<strong>不要求</strong>读入所有的数据后再全部输出, 您可以读入一组数据后输出一组答案, 或者读入全部数据后输出所有答案. 您可以自由选择您喜欢的方法完成题目, 只要您的程序所有输出都正确即可. 例如该题中, 您可以读入 5 后输出 15, 再读入 10 后输出 55, 等等; 也可以读入 5, 10, 20 后输出 15, 55, 210.</li>
</ol>

